# 문자열 계산기 구현 계획

## 📋 프로젝트 개요

입력한 문자열에서 숫자를 추출하여 더하는 계산기를 TDD 방식으로 구현합니다.

---

## 🏗️ 폴더 구조 (MVC 패턴 기반)

```
src/
├── models/                  # Model: 비즈니스 로직 및 데이터 처리
│   ├── Calculator.js        # 계산 로직
│   └── StringParser.js      # 문자열 파싱 로직
├── views/                   # View: 입출력 담당
│   ├── InputView.js         # 사용자 입력
│   └── OutputView.js        # 결과 출력
├── controllers/             # Controller: Model과 View 연결
│   └── CalculatorController.js
├── App.js                   # 애플리케이션 진입점
└── index.js                 # 메인 실행 파일

__tests__/
├── models/
│   ├── Calculator.test.js
│   └── StringParser.test.js
├── controllers/
│   └── CalculatorController.test.js
└── ApplicationTest.js
```

### MVC 역할 분담
- **Model**: 비즈니스 로직, 데이터 처리, 유효성 검증
- **View**: 사용자 입력 받기, 결과 출력 (Console I/O)
- **Controller**: Model과 View를 조율하여 애플리케이션 흐름 제어

---

## ✅ 구현 기능 목록

### 1단계: 기본 입출력 구현
- [x] 사용자에게 "덧셈할 문자열을 입력해 주세요." 메시지 출력
- [ ] 사용자로부터 문자열 입력 받기
- [ ] 입력받은 문자열을 그대로 출력 (초기 검증용)

---

### 2단계: 빈 문자열 처리
- [ ] 테스트: 빈 문자열("") 입력 시 0 반환
- [ ] 구현: 빈 문자열 처리 로직

---

### 3단계: 단일 숫자 처리
- [ ] 테스트: 숫자 하나만 입력 시 해당 숫자 반환 (예: "1" => 1)
- [ ] 구현: 단일 숫자 파싱 로직

---

### 4단계: 기본 구분자(쉼표) 처리
- [ ] 테스트: 쉼표로 구분된 두 숫자 합산 (예: "1,2" => 3)
- [ ] 테스트: 쉼표로 구분된 세 숫자 합산 (예: "1,2,3" => 6)
- [ ] 구현: 쉼표 구분자 파싱 로직
- [ ] 구현: 숫자 배열 합산 로직

---

### 5단계: 기본 구분자(콜론) 처리
- [ ] 테스트: 콜론으로 구분된 숫자 합산 (예: "1:2:3" => 6)
- [ ] 구현: 콜론 구분자 파싱 로직

---

### 6단계: 혼합 기본 구분자 처리
- [ ] 테스트: 쉼표와 콜론 혼합 사용 (예: "1,2:3" => 6)
- [ ] 구현: 복수 기본 구분자 처리 로직

---

### 7단계: 기본 구분자 외 문자 입력 시 예외 처리
- [ ] 테스트: 기본 구분자 이외 문자 입력 시 "[ERROR]"로 시작하는 Error 발생
  - 예: "1,2@3" => Error
  - 예: "1abc2" => Error
- [ ] 구현: 잘못된 문자 검증 로직

---

### 8단계: 커스텀 구분자 파싱
- [ ] 테스트: 커스텀 구분자 추출 (예: "//;\n1;2;3" => 구분자는 ";")
- [ ] 구현: "//"와 "\n" 사이의 커스텀 구분자 추출 로직

---

### 9단계: 커스텀 구분자로 숫자 분리 및 합산
- [ ] 테스트: 커스텀 구분자로 숫자 합산 (예: "//;\n1;2;3" => 6)
- [ ] 테스트: 다른 커스텀 구분자 (예: "//|\n1|2|3" => 6)
- [ ] 구현: 커스텀 구분자 기반 파싱 로직

---

### 10단계: 커스텀 구분자 외 문자 입력 시 예외 처리
- [ ] 테스트: 커스텀 구분자 지정 후 다른 문자 사용 시 Error 발생
  - 예: "//;\n1;2,3" => Error (쉼표 사용 불가)
  - 예: "//;\n1;2:3" => Error (콜론 사용 불가)
- [ ] 구현: 커스텀 구분자 전용 검증 로직

---

### 11단계: 음수 입력 예외 처리
- [ ] 테스트: 음수 입력 시 "[ERROR]"로 시작하는 Error 발생
  - 예: "-1,2,3" => Error
  - 예: "1,-2,3" => Error
- [ ] 구현: 양수 검증 로직

---

### 12단계: 결과 출력 형식 구현
- [ ] 테스트: 결과를 "결과 : {숫자}" 형식으로 출력
- [ ] 구현: 결과 포맷팅 로직

---

### 13단계: 전체 통합 테스트
- [ ] 테스트: 빈 문자열 입력 → "결과 : 0" 출력
- [ ] 테스트: "1,2:3" 입력 → "결과 : 6" 출력
- [ ] 테스트: "//;\n1;2;3" 입력 → "결과 : 6" 출력
- [ ] 테스트: 잘못된 입력 시 Error 발생 및 애플리케이션 종료

---

### 14단계: 리팩토링
- [ ] 구조적 개선: 중복 코드 제거
- [ ] 구조적 개선: 함수/메서드 분리 (단일 책임 원칙)
- [ ] 구조적 개선: 변수명/함수명 명확화
- [ ] 구조적 개선: 매직 넘버/문자열 상수화

---

## 📝 TDD 사이클

각 기능은 다음 순서로 구현합니다:

1. **Red**: 실패하는 테스트 작성
2. **Green**: 테스트를 통과하는 최소한의 코드 구현
3. **Refactor**: 테스트가 통과한 상태에서 코드 개선

---

## 🔍 커밋 전략

- 각 단계의 테스트 작성 후 커밋 (Red)
- 테스트 통과 후 커밋 (Green)
- 리팩토링 후 별도 커밋 (Refactor)
- 커밋 메시지는 기능 단위로 명확하게 작성

예시:
```
test: 빈 문자열 입력 시 0 반환 테스트 추가
feat: 빈 문자열 처리 로직 구현
refactor: 문자열 파싱 로직 함수 분리
```

---

## ⚠️ 주의사항

1. **양수만 허용**: 음수 입력 시 Error 발생
2. **Error 메시지**: 모든 예외는 "[ERROR]"로 시작
3. **애플리케이션 종료**: Error 발생 시 애플리케이션 종료
4. **기본 구분자**: 쉼표(,), 콜론(:)
5. **커스텀 구분자 형식**: `//[구분자]\n[숫자들]`
6. **커스텀 구분자 사용 시**: 기본 구분자 사용 불가

---

## 🎯 완료 조건

- [ ] 모든 테스트 통과
- [ ] ESLint 오류 0개
- [ ] 코드 커버리지 충분
- [ ] MVC 패턴 구조 준수
  - [ ] Model: 비즈니스 로직과 데이터 처리만 담당
  - [ ] View: 입출력만 담당 (비즈니스 로직 없음)
  - [ ] Controller: Model과 View 조율만 담당
- [ ] 의미 있는 커밋 히스토리
